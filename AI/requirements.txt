flask
pandas
opencv-python
 Pillow # PIL.Image의 현재 패키지명
# Hugging Face Transformers (모델 로딩 및 실행용)
transformers
torch
torchvision
# sentencepiece # 일부 트랜스포머 모델에 필요할 수 있음

# PaddleOCR (ocr_paddle.py에서 사용)
paddlepaddle # 또는 paddlepaddle-gpu (GPU 사용 시)
paddleocr

# 압축 해제 (decompression.py에서 사용)
rarfile

# YOLOv5 또는 YOLOv10 실행에 필요한 라이브러리 (ultralytics 등)
# ultralytics # yolov5, yolov8 등 포함. yolov10도 지원할 수 있음.

# 기타 필요할 수 있는 라이브러리
# numpy # opencv, pandas 등의 의존성으로 보통 함께 설치됨
# scikit-learn 